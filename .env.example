# Real-Time AI Model Verification Network Environment Configuration

# =============================================================================
# ENHANCED CONFIGURATION SYSTEM - SECURITY VALIDATED
# =============================================================================

# ⚠️  CRITICAL SECURITY VARIABLES - REQUIRED FOR STARTUP
# These MUST be set - application will not start without them

# Blockchain Private Key (NEVER use default/test keys in production)
PRIVATE_KEY=0xYOUR_ACTUAL_PRIVATE_KEY_HERE

# Blockchain RPC URL (NEVER use localhost in production)  
RPC_URL=https://your-ethereum-rpc-endpoint.com

# Network identifier (NEVER use localhost in production)
NETWORK_ID=mainnet

# =============================================================================
# NITROLITE SDK CONTRACT ADDRESSES
# =============================================================================
# Contract addresses for Nitrolite ERC-7824 State Channels
# ⚠️  IMPORTANT: Use actual deployed contract addresses for production

# Custody contract address (holds channel funds) - Yellow Network Testnet
CUSTODY_CONTRACT_ADDRESS=0x742d35cc8c4cc8e1c8cb1b33df5a9b3c2bdc4f2e

# Guest contract address (participant proxy) - Yellow Network Testnet  
GUEST_CONTRACT_ADDRESS=0x8b3a3b2f52c7e5d9a1b4c6e8f2a9c3d5b7e1f4a6

# Adjudicator contract address (dispute resolution) - Yellow Network Testnet
ADJUDICATOR_CONTRACT_ADDRESS=0x1a2b3c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9a0b

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Server port (defaults to 3000 if not set)
PORT=3000

# Server host (use 0.0.0.0 for Docker, localhost for local dev)
SERVER_HOST=localhost

# API route prefix
API_PREFIX=/api

# =============================================================================
# BLOCKCHAIN CONFIGURATION  
# =============================================================================
# Ethereum chain ID (1 = mainnet, 5 = goerli, etc.)
CHAIN_ID=1

# Additional blockchain settings
BLOCKCHAIN_PORT=8545
METRICS_PORT=9090

# =============================================================================
# PERFORMANCE TUNING
# =============================================================================
MAX_CONCURRENT_VERIFICATIONS=50
PROCESSING_INTERVAL_MS=100
REQUEST_TIMEOUT_MS=30000
SHUTDOWN_TIMEOUT_MS=30000

# =============================================================================
# MONITORING & OBSERVABILITY
# =============================================================================
PROMETHEUS_PORT=9091
GRAFANA_PORT=3001
LOG_LEVEL=info
ENABLE_METRICS=true

# =============================================================================
# DEVELOPMENT SETTINGS (LOCAL ONLY)
# =============================================================================
HOT_RELOAD=false
NODE_ENV=development
ENABLE_SIMULATION_MODE=false
DEBUG_MODE=false
NODE_ENV=development

# =============================================================================
# LEGACY CONFIGURATION (for backward compatibility)
# =============================================================================
# Network Configuration
RPC_URL=https://eth.llamarpc.com

# Nitrolite SDK Configuration
NITROLITE_API_KEY=your_nitrolite_api_key_here
NITROLITE_NETWORK=ethereum

# State Channel Configuration
CHANNEL_CAPACITY=1000
BATCH_SIZE=10
GASLESS_ENABLED=true
CROSS_CHAIN_ENABLED=true

# Performance Targets
MAX_VERIFICATION_LATENCY=1000
MIN_THROUGHPUT=100
GAS_EFFICIENCY_TARGET=90

# Evolutionary Configuration
EVOLUTIONARY_MODE=true
OPTIMIZATION_INTERVAL=60000

# Server Configuration (legacy)
PORT=3000
HOST=localhost

# Security
PRIVATE_KEY=your_private_key_here
VERIFICATION_SIGNATURE_KEY=your_verification_key_here

# State Channel Configuration
ADJUDICATOR_ADDRESS=0x1234567890123456789012345678901234567890
DEFAULT_PARTICIPANT_1=0xabcdefabcdefabcdefabcdefabcdefabcdefabcdef
DEFAULT_PARTICIPANT_2=0x1111111111111111111111111111111111111111

# Cross-Chain Bridge Configuration
BRIDGE_CONNECTION_DELAY_MS=100
CHAIN_PROCESSING_DELAY_MS=50

# Performance Configuration
SHUTDOWN_TIMEOUT_MS=30000